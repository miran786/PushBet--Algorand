---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Dev-Mode Test User Selector

## Objective
Provide a set of quick-login buttons on the sign-in page to switch between multiple test users (Campus Champ, Fitness Freak, Clean Monitor, Dev Admin) for easier manual testing.

## Context
- .gsd/SPEC.md
- src/app/pages/Login.tsx
- server/seed-db.js
- server/controllers/userController.js

## Tasks

<task type="auto">
  <name>Update seed-db.js with passwords</name>
  <files>
    <file>server/seed-db.js</file>
  </files>
  <action>
    - Ensure all users in the `users` array have the `password` field set to `hashedPassword`.
    - `hashedPassword` should be generated using `bcrypt.hash("password123", 10)`.
  </action>
  <verify>node server/seed-db.js</verify>
  <done>Console output says "Database seeded successfully!" and no errors occur.</done>
</task>

<task type="auto">
  <name>Implement Quick Login UI in Login.tsx</name>
  <files>
    <file>src/app/pages/Login.tsx</file>
  </files>
  <action>
    - Add a "Quick Login" section below the main form.
    - Create four buttons with the styling derived from the existing DEV LOGIN button.
    - Map buttons to:
      - `champ@campus.edu`
      - `fitness@campus.edu`
      - `monitor@campus.edu`
      - `test@test.com`
    - Update the click handler to call a helper function that sets email/password and submits the form.
  </action>
  <verify>Check browser for new buttons on /login page.</verify>
  <done>User can see and click buttons to log in as different users.</done>
</task>

## Success Criteria
- [ ] Users can log in as any of the four test accounts via one-click buttons.
- [ ] Database is correctly seeded with passwords for all test accounts.
